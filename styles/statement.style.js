import styled from "styled-components";

const StatementStyle = styled.div`
  .statement-container {
    display: block;
  }

  .statement-content {
    color: red;
  }
`;

export default StatementStyle;
